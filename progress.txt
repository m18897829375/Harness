# Ralph Progress Log
Started: Wed Jul  8 23:10:44     2026
---

## 2026-07-08 23:55 - US-001 智能搜索按钮状态持久化（方案D：onElementRemoval 即刻重插）
- What was implemented:
  - Refactored workspace/deepseek-websearch-extension/content.js to follow smart-search-button-design.md.
  - Button className changed to `web-search-btn-smart-search`; aria-label/title/span text now all `智能搜索`.
  - Button innerHTML uses `SMART_SEARCH_ICON_SVG` (16x16 globe icon with currentColor) + `<span>智能搜索</span>`.
  - Added full inactive/hover/active CSS via `injectStyles()` into `<style id="web-search-styles">`, idempotent.
  - `aria-disabled` is always `"false"`; button remains clickable in all states.
  - `toggleSmartSearchActive()` syncs `document.body.dataset.ralphSmartSearch` to `'active'` or `''`.
  - `removeButton()` only detaches DOM and clears timers; preserves `isSmartSearchActive`.
  - `injectButton()` re-applies `.active` class and `aria-pressed="true"` when state is active.
  - `setButtonIdle()` restores idle innerHTML and re-syncs `.active` based on `isSmartSearchActive`.
  - Added `setupButtonRemovalObserver()` using MutationObserver watching parent container `childList+subtree`; reinserts button via `setTimeout(fn,0)` when removed.
  - `setButtonLoading()` and `setButtonError()` use Chinese text matching the spec and keep `aria-disabled="false"`.
- Files changed: workspace/deepseek-websearch-extension/content.js
- Verification:
  - `npx tsc --noEmit` passes.
  - `node scripts/us-001-verify.js` (JSDOM self-check) passes: button exists, class/aria/text/styles match, active toggle works, reinsertion after DOM removal preserves active state, style injection is idempotent.
  - code-reviewer subagent reviewed; addressed feedback by calling `updateButtonState()` in `setButtonIdle()` and observing grandparent in `setupButtonRemovalObserver()` for parent-replacement resilience.
- **Learnings for future iterations:**
  - The design spec examples for loading/error state set `aria-disabled="true"`, but the acceptance criteria explicitly require `aria-disabled` always `"false"`. In conflicts, the locked contract AC takes precedence.
  - JSDOM with a mocked `chrome` runtime object is an effective lightweight regression harness for content-script DOM behavior before Playwright browser tests.
---

## 2026-07-09 00:45 - US-001 智能搜索按钮状态持久化（方案D：onElementRemoval 即刻重插）—— Retry 2 (QA round 2/3)
- What was fixed (per evaluation feedback):
  - Removed all `console.log` / `console.warn` statements from `content.js` (CRITICAL code-quality issue). Replaced with inline comments explaining the silent-fallback intent.
  - Added explicit MutationObserver lifecycle helpers: `disconnectObserver`, `disconnectButtonRemovalObserver`, `clearTimers`, and `teardown`.
  - Scoped `setupButtonRemovalObserver()` to the button's **direct parent** (`parent`) instead of `parent.parentElement`, reducing observation surface and avoiding infinite re-insert loops.
  - `removeButton()` now calls `disconnectButtonRemovalObserver()` and `clearTimers()`; persistent `isSmartSearchActive` is still preserved.
  - `init()` registers `window.addEventListener('beforeunload', teardown)` so SPA observer and timers are cleaned on page unload.
  - `setButtonLoading()` now clears any pending `errorRevertTimer` before entering loading state, preventing stale timers from corrupting button state.
  - Fixed JSDoc that incorrectly referenced a non-existent `'success'` button state; state machine is now documented as `idle → loading → error`.
- Verification:
  - `npx tsc --noEmit` in `workspace/deepseek-websearch-extension` passes (exit 0).
  - `node scripts/us-001-verify.js` passes all 21 checks: button existence, class/aria/text/styles, active toggling, DOM removal reinsertion with active state preserved, idempotent style injection.
- Code review:
  - `code-reviewer` subagent reviewed the diff and reported 0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW issues. Verdict: APPROVE.
- Files changed: `workspace/deepseek-websearch-extension/content.js`
- **Learnings for future iterations:**
  - Even silent-failure comments should not use `console.*` in production code; project rules treat any `console.log` in shipped source as CRITICAL.
  - When using MutationObserver to guard against DOM removal, observe the **minimal subtree** (direct parent) and always pair with `disconnect()` helper to avoid observer leaks and race conditions during React re-renders.
  - Always clear the previous state's auto-revert timer when transitioning into a new state machine state (e.g., loading), not only in the state that created it (error).
---

## 2026-07-09 13:05 - US-002 快速模式历史对话排除扩展按钮
- What was implemented:
  - Modified `isExpertMode()` in `workspace/deepseek-websearch-extension/content.js` to detect native DeepSeek quick-mode search toggles on history pages.
  - When no `div[role='radio']` elements exist and `lastKnownMode === 'expert'`, the function calls `findToolbarContainer()` and inspects `.ds-toggle-button` elements.
  - If a toggle button's `textContent` is `'联网搜索'` or contains `'搜索'`, the function logs the detection and returns `false`, preventing the extension smart-search button from being injected.
  - If the toolbar container is present but has no `.ds-toggle-button` (toolbar not yet rendered), the function trusts `lastKnownMode` and returns `true`.
  - Existing `div[role='radio']` index-1 `aria-checked` detection logic is preserved unchanged.
  - Widened JSDoc type of `lastKnownMode` from `null | 'expert' | 'instant'` to `string | null` to avoid TypeScript narrowing errors when comparing against `'expert'` in the fallback branch.
- Files changed:
  - `workspace/deepseek-websearch-extension/content.js`
  - `workspace/deepseek-websearch-extension/scripts/us-002-verify.js` (new JSDOM self-check)
  - `workspace/deepseek-websearch-extension/scripts/us-002-playwright-smoke.js` (new Playwright smoke test via local test-page)
- Verification:
  - `npx tsc --noEmit` in `workspace/deepseek-websearch-extension` passes.
  - `node scripts/us-002-verify.js` passes all 5 cases: radio Expert, radio Instant, quick-mode history with native toggle, expert history with unrendered toolbar, instant mode history.
  - `node scripts/us-002-playwright-smoke.js` passes: Expert mode injects 1 button, Instant mode removes it, toggling back to Expert re-injects it.
- Subagent review:
  - `code-reviewer`: WARN — 1 MEDIUM (`console.log` remains in `isExpertMode()` because the locked acceptance criteria explicitly require logging the detection; contract-bound behavior), 1 LOW (string matching may be broad, but matches the locked contract). No CRITICAL or HIGH.
  - `e2e-runner`: PASS — TypeScript, JSDOM self-check, and Playwright smoke check all pass.
- [BM25] skills=(none relevant), cli=(npx tsc, npm test, opencli playwright)
- [SELF-CHECK]: typecheck ✓, JSDOM ✓, Playwright smoke ✓, code-review subagent ✓, e2e-runner subagent ✓
- [SCOPE]: Only `isExpertMode()` and related `lastKnownMode` JSDoc changed in `content.js`; verification scripts are auxiliary and do not alter UI behavior.

